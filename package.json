{
  "name": "dino-api",
  "version": "0.0.1",
  "devDependencies": {
    "@babel/preset-env": "^7.18.0",
    "babel-jest": "^28.1.0",
    "jest": "^28.1.0",
    "wrangler": "2.0.5"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "publish": "wrangler publish",
    "test": "jest ./test"
  },
  "jest": {
    "transform": {
      "^.+\\.[t|j]sx?$": "babel-jest"
    }
  },
  "dependencies": {
    "url": "^0.11.0"
  }
}

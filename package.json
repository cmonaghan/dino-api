{
  "name": "dino-api",
  "version": "0.0.1",
  "devDependencies": {
    "@babel/preset-env": "^7.19.0",
    "babel-jest": "^29.0.3",
    "jest": "^29.0.3",
    "wrangler": "^2.0.29"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "publish": "wrangler publish",
    "test": "jest ./test",
    "test-watch": "jest ./test --watch"
  },
  "jest": {
    "transform": {
      "^.+\\.[t|j]sx?$": "babel-jest"
    }
  },
  "dependencies": {
    "url": "^0.11.0"
  }
}
